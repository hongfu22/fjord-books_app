<p><%= t('devise.email.welcome') %> <%= @email %>!</p>

<p><%= t('devise.email.confirm_email') %></p>

<p><%= link_to t('devise.email.confirm_account'), confirmation_url(@resource, confirmation_token: @token) %></p>
